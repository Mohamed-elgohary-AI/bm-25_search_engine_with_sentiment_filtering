model_name: distilbert-base-uncased
num_labels: 2
max_length: 128      # down from 256
batch_size: 128      # bigger batch = fewer steps = faster
epochs: 3            
learning_rate: 2e-5  
train_split: 0.9     
sample_size: 50000   # down from 300000
n_splits: 3          # down from 5
fold: 0