# docker-compose.yml
services:
  search-api:
    build: .
    ports: ["8000:8000"]
    volumes:
      - ./artifacts:/app/artifacts

  mlflow:
    image: ghcr.io/mlflow/mlflow:v2.11.3
    ports: ["5000:5000"]
    command: mlflow server --host 0.0.0.0
    volumes:
      - ./mlruns:/mlruns